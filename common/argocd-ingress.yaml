apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    external-dns.alpha.kubernetes.io/hostname: "argo3.haenu.xyz"
spec:
  ingressClassName: "nginx"
  rules:
  - host: argo3.haenu.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-argo-cd-server
            port:
              name: https

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: argocd-argo-cd-server-ingress
#   namespace: argocd
#   annotations:
#     kubernetes.io/ingress.class: nginx
#     nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
#     external-dns.alpha.kubernetes.io/hostname: argo2.haenu.xyz
# spec:
#   rules:
#   - host: argo2.haenu.xyz
#     http:
#       paths:
#       - pathType: Prefix
#         path: "/"
#         backend:
#           service:
#             name: argocd-argo-cd-server
#             port:
#               number: 80
